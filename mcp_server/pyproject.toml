[project]
name = "mcp-local-files"
version = "0.1.0"
description = "Read-only MCP server exposing files under a configured set of local folders."
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[tool.uv]
package = false
